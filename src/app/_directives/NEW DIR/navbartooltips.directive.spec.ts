import { NavbartooltipsDirective } from './navbartooltips.directive';

describe('NavbartooltipsDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbartooltipsDirective();
    expect(directive).toBeTruthy();
  });
});
